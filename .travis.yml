language: csharp
mono: none
sudo: required
dotnet: 1.0.1
dist: trusty
addons:
  apt:
    sources:
    - sourceline: 'deb [arch=amd64] https://apt-mo.trafficmanager.net/repos/dotnet-release/ trusty main'
      key_url: 'https://apt-mo.trafficmanager.net/keys/microsoft.asc'
    packages:
    - dotnet-dev-1.0.1
before_script:
  dotnet restore Skillustrator2017.sln

script:
  dotnet build Skillustrator2017.sln
...